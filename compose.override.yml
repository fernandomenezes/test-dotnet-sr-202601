services:
  sqlserver:
    volumes:
      - ./.containers/sqlserver:/var/opt/mssql/data
    ports:
      - "1433:1433"
    environment:
      - ACCEPT_EULA=Y
      - SA_PASSWORD=SenhaForte(!)Senha123
